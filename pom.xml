<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>bmc.dev.resources.code</groupId>
        <artifactId>bmc-resources-parent</artifactId>
        <version>1.0.1</version>
    </parent>

    <artifactId>bmc-resources</artifactId>
    <version>bmc-local</version>
    <packaging>jar</packaging>

    <name>BMC - BMC Resources</name>
    <description>Resources comprising code style, checkstyle, ignore files et als, common for all projects.</description>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-resources</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${basedir}/target/classes</outputDirectory>
                            <includeEmptyDirs>false</includeEmptyDirs>
                            <addDefaultExcludes>false</addDefaultExcludes>
                            <!-- These resources will be included in the final JAR produced by this project -->
                            <!-- They are used across all projects, don't include one-off requirements -->
                            <resources>
                                <!-- Files to include -->
                                <resource>
                                    <directory>${basedir}</directory>
                                    <includes>
                                        <include>.gitignore</include>
                                        <include>.dockerignore</include>
                                        <include>.editorconfig</include>
                                        <include>checkstyle.xml</include>
                                        <include>LICENSE</include>
                                        <include>mvnw</include>
                                    </includes>
                                </resource>
                                <!-- Folders to include -->
                                <resource>
                                    <directory>${basedir}/templates/.github</directory>
                                    <targetPath>.github</targetPath>
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                </resource>
                                <resource>
                                    <directory>${basedir}/templates/.mvn</directory>
                                    <targetPath>.mvn</targetPath>
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                    <excludes>
                                        <exclude>**/settings.xml</exclude>
                                    </excludes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
